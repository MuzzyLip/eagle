// /pkg/version 包是用来展示项目版本信息的
// 在make build时会注入版本信息，后续可以在命令行中通过 --version 参数来展示版本信息
/**
  ldflags注入的工作方式是：
  1. Go 先编译源码。
  2. 到链接阶段时，-ldflags 把参数传给 linker。
  3. -X 包路径.变量名=值 会直接改写这个包级变量的最终值。
  4. 所以程序启动后，读到的就不是源码里的默认值，而是构建时注入进去的新值。
*/
package version
